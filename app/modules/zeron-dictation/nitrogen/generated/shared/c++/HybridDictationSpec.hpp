///
/// HybridDictationSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `DictationSupport` to properly resolve imports.
namespace margelo::nitro::zremote::dictation { struct DictationSupport; }
// Forward declaration of `DictationModelState` to properly resolve imports.
namespace margelo::nitro::zremote::dictation { enum class DictationModelState; }

#include "DictationSupport.hpp"
#include <NitroModules/Promise.hpp>
#include "DictationModelState.hpp"
#include <string>
#include <functional>
#include <optional>

namespace margelo::nitro::zremote::dictation {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Dictation`
   * Inherit this class to create instances of `HybridDictationSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridDictation: public HybridDictationSpec {
   * public:
   *   HybridDictation(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridDictationSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridDictationSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridDictationSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<DictationSupport>> isSupported() = 0;
      virtual std::shared_ptr<Promise<DictationModelState>> modelState(const std::string& locale) = 0;
      virtual std::shared_ptr<Promise<void>> downloadModel(const std::string& locale, const std::function<void(double /* progress */)>& onProgress) = 0;
      virtual std::shared_ptr<Promise<void>> start(const std::optional<std::string>& locale, const std::function<void(const std::string& /* text */)>& onPartial, const std::function<void(const std::string& /* text */)>& onFinal, const std::function<void(const std::string& /* message */)>& onError) = 0;
      virtual std::shared_ptr<Promise<void>> stop() = 0;
      virtual std::shared_ptr<Promise<void>> cancel() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Dictation";
  };

} // namespace margelo::nitro::zremote::dictation
