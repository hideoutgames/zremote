// Expo Go preview shim — not used in production builds.
// @expo/ui/swift-ui/modifiers — widget-layout modifiers; identity stubs.

type Modifier = unknown;
const m = (...args: unknown[]): Modifier => args;

export const font = m;
export const foregroundStyle = m;
export const padding = m;
export const progressViewStyle = m;
export const tint = m;
export const frame = m;
export const background = m;
export const foregroundColor = m;
export const imageScale = m;
export const bold = m;

export default {};
